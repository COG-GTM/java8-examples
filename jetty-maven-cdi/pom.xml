<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>eu.infomas.examples</groupId>
        <artifactId>parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>jetty-maven-cdi</artifactId>
    <packaging>war</packaging>

    <name>jetty-maven-cdi</name>

    <properties>
        <jetty.version>12.0.21</jetty.version>
        <weld.version>5.1.7.Final</weld.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <version>6.0.0</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.enterprise</groupId>
            <artifactId>jakarta.enterprise.cdi-api</artifactId>
            <version>4.0.1</version>
            <scope>provided</scope>
            <exclusions>
                <exclusion>
                    <groupId>jakarta.el</groupId>
                    <artifactId>jakarta.el-api</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>jakarta.annotation</groupId>
            <artifactId>jakarta.annotation-api</artifactId>
            <version>2.1.1</version>
            <scope>provided</scope>
        </dependency>
        <!--
        Weld (CDI 4.0 reference implementation), packaged as an all-in-one shaded
        jar compatible with Jakarta EE 10 (Servlet 6, CDI 4). Unlike Jetty 9, in
        Jetty 12 the webapp classloader isolates the webapp from server/plugin
        classes, so Weld must live on the webapp classpath (runtime scope) for its
        ServletContainerInitializer to bootstrap CDI and for the JNDI BeanManager
        resource-env-ref type to resolve.
        -->
        <dependency>
            <groupId>org.jboss.weld.servlet</groupId>
            <artifactId>weld-servlet-shaded</artifactId>
            <version>${weld.version}</version>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.4.0</version>
                <configuration>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.eclipse.jetty.ee10</groupId>
                <artifactId>jetty-ee10-maven-plugin</artifactId>
                <version>${jetty.version}</version>
                <configuration>
                    <!-- Paths are anchored to ${project.basedir} so that the plugin works
                         both from the module directory (mvn jetty:run) and from the reactor
                         root (mvn -pl jetty-maven-cdi jetty:run). The default-named
                         WEB-INF/jetty-env.xml is picked up automatically. -->
                    <webApp>
                        <overrideDescriptor>${project.basedir}/src/main/webapp/WEB-INF/web-overwrite.xml</overrideDescriptor>
                    </webApp>
                    <contextXml>${project.basedir}/src/main/webapp/WEB-INF/jetty-context.xml</contextXml>
                </configuration>
                <dependencies>
                    <!-- Jetty CDI integration: provides the CdiServletContainerInitializer
                         that installs the CdiDecoratingListener so that Weld's decorator
                         is used to inject Servlets, Filters and Listeners. This is the
                         Jetty 12 replacement for the Jetty 9 'cdi-decorate' module. -->
                    <dependency>
                        <groupId>org.eclipse.jetty.ee10</groupId>
                        <artifactId>jetty-ee10-cdi</artifactId>
                        <version>${jetty.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>

</project>
