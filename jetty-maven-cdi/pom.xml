<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>eu.infomas.examples</groupId>
        <artifactId>parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>jetty-maven-cdi</artifactId>
    <packaging>war</packaging>

    <name>jetty-maven-cdi</name>

    <dependencies>
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
            <version>3.1.0</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>javax.enterprise</groupId>
            <artifactId>cdi-api</artifactId>
            <version>1.2</version>
            <scope>provided</scope>
            <exclusions>
                <exclusion>
                    <groupId>javax.el</groupId>
                    <artifactId>javax.el-api</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <!--
        javax.annotation (JSR-250, e.g. @PostConstruct) was part of the JDK
        through Java 8 but removed from Java 11. It is provided at runtime by
        the Weld/Jetty container, so it is only needed to compile against.
        -->
        <dependency>
            <groupId>javax.annotation</groupId>
            <artifactId>javax.annotation-api</artifactId>
            <version>1.3.2</version>
            <scope>provided</scope>
        </dependency>
        <!--
        Weld (CDI reference implementation) is packaged into WEB-INF/lib so the
        webapp classloader can see the CDI implementation directly. With the
        Jetty 9.2 plugin this lived only on the plugin classpath; Jetty 9.4's
        stricter classloader isolation requires it inside the war.
        -->
        <dependency>
            <groupId>org.jboss.weld.servlet</groupId>
            <artifactId>weld-servlet</artifactId>
            <version>2.4.8.Final</version>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <configuration>
                    <failOnMissingWebXml>false</failOnMissingWebXml>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.eclipse.jetty</groupId>
                <artifactId>jetty-maven-plugin</artifactId>
                <version>9.4.53.v20231009</version>
                <configuration>
                    <!--
                    No need to explicitly configure this if using default name 'jetty-env.xml'
                    <jettyEnvXml>src/main/webapp/WEB-INF/jetty-env.xml</jettyEnvXml>
                    -->
                    <webApp>
                        <overrideDescriptor>src/main/webapp/WEB-INF/web-overwrite.xml</overrideDescriptor>
                    </webApp>
                    <contextXml>src/main/webapp/WEB-INF/jetty-context.xml</contextXml>
                    <!--
                    jetty-maven-plugin 9.2 wired Jetty's JNDI provider implicitly;
                    9.4 requires these to be set so jetty-env.xml / env-entries resolve.
                    -->
                    <systemProperties>
                        <systemProperty>
                            <name>java.naming.factory.initial</name>
                            <value>org.eclipse.jetty.jndi.InitialContextFactory</value>
                        </systemProperty>
                        <systemProperty>
                            <name>java.naming.factory.url.pkgs</name>
                            <value>org.eclipse.jetty.jndi</value>
                        </systemProperty>
                    </systemProperties>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
